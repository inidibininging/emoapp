the following bugs in this feauture should be fixed:
- when starting the mindmap section, clicking in order tk create a new idea, opens up a screen where the button for crearing the idea is not clickable due to an overlap in the ui/ interface. Move  the action buttons for creating editong etc to the top for this function.
- after creating an idea, there is a red screen that pops up. It seems that  is missing.
- after creation of an idea, no ide  is visible
- moving / zooming  across the mindmap is not possible
- there are no coordinates for shpwing which zone the viewport is focused on. 
